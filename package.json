{
  "name": "budgetier",
  "version": "1.0.0",
  "private": true,
  "description": "Personal single-user budgeting web app (Vue + Azure Functions + Cosmos DB)",
  "scripts": {
    "dev": "swa start",
    "build": "npm run build --prefix frontend && npm run build --prefix api",
    "install:all": "npm install --prefix frontend && npm install --prefix api",
    "seed": "npm run seed --prefix api"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.2"
  }
}
